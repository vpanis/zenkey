<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-4 col-sm-offset-4">
      <div class="form-devise">
        <h2>Renvoyez-moi les instructions pour débloquer mon compte</h2>

        <%= simple_form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
          <%= f.error_notification %>
          <%= f.full_error :unlock_token %>

          <div class="form-inputs">
            <%= f.input :email, placeholder:"dupont@gmail.com", required: true, autofocus: true %>
          </div>

          <div class="form-actions">
            <%= f.button :submit, "Renvoyez-moi les instructions instructions", class:"btn btn-red" %>
          </div>
        <% end %>

        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>
