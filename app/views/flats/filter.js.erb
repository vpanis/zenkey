<% if @flat.errors.any? %>
  alert("Vos modifications n'ont pas été enregistrées.");
<% else %>
  $('#message-modif').text(' <%= current_user.email %> Vos modifications ont bien été enregistrées.');
  $('#candidates').html(" <%= j render 'candidates', bookings_pending: @bookings_pending, bookings_confirmed: @bookings_confirmed, bookings_cancelled: @bookings_cancelled %> ");
<% end %>
