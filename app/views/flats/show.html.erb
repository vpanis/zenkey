<% content_for(:after_js) do %>
  <%= javascript_tag do %>
    $(document).ready(function() {
      var handler = Gmaps.build('Google');
      handler.buildMap({ internal: { id: 'map' } }, function() {
        markers = handler.addMarkers(<%= raw @hash.to_json %>);
        handler.bounds.extendWith(markers);
        handler.fitMapToBounds();
        if (markers.length == 0) {
          handler.getMap().setZoom(2);
        } else if (markers.length == 1) {
          handler.getMap().setZoom(14);
        }
      });
    });
  <% end %>
<% end %>


<div class="container">
  <div class="row">
    <div class="col-xs-12 col-md-4"><h2>Loyer: <%= @flat.rent%>€</h2></div>
  </div>
    <!-- Colonne de gauche -->
  <div class="row">
    <div class="col-xs-12 col-md-8">
      <%= cl_image_tag(@flat.photos.first.path, :alt => "banner_image", height:300, width: 720, crop: :fill) %>
      <h2><%= @flat.title%></h2>
      <br>
      <h4>DESCRIPTION</h4>
      <hr>
      <p><%= @flat.description %></p>
      <br>
      <h4>INFOS</h4>
      <hr>
      <ul>
        <li>Adresse: <%= @flat.address %></li>
        <li>Disponible: <%= @flat.availability_date %></li>
        <li><%= @flat.description %></li>
      </ul>
    </div>
    <!-- Colonne de droite -->
    <div class="col-xs-12 col-md-4" id="sidebar">
      <div id="map" style="width: 100%; height: 300px;"></div>
      <h3>Prochaines visites</h3>
      <%= render 'pickvisitform' %>
    </div>
  </div>
</div>


