<div class="modal fade" tabindex="-1" role="dialog" id="modal-confirm-bookings<%= booking.id %>">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title">Confirmez votre choix !</h4>
      </div>

        <% if booking.status == "Pending" %>

          <div class="modal-body">
            <p>Vous êtes sur le point de choisir ce locataire.</p>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-default" data-dismiss="modal">Retour</button>

            <%= simple_form_for [@flat, booking], url: flat_booking_booking_confirm_path(@flat, booking), method: :patch do |f| %>
              <%= f.submit "Choisir ce locataire !", class: "btn btn-red" %>
            <% end %>

          </div>

        <% elsif booking.status == "Confirmed" %>

          <div class="modal-body">
            <p>Vous êtes sur le point d'annuler cette réservation.</p>
            <p>Si vous confirmez, vous pourrez changer de locataire.</p>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-default" data-dismiss="modal">Retour</button>

            <%= simple_form_for [@flat, booking], url: flat_booking_booking_cancel_path(@flat, booking), method: :patch do |f| %>
              <%= f.submit "J'annule cette réservation !", class: "btn btn-red" %>
            <% end %>

          </div>

        <% else %>

          <div class="modal-body">
            <p>Vous êtes sur le point de choisir ce locataire.</p>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-default" data-dismiss="modal">Retour</button>

            <%= simple_form_for [@flat, booking], url: flat_booking_booking_confirm_path(@flat, booking), method: :patch do |f| %>
              <%= f.submit "Choisir ce locataire !", class: "btn btn-red" %>
            <% end %>

          </div>

        <% end %>

    </div><!-- /.modal-content -->
  </div><!-- /.modal-dialog -->
</div><!-- /.modal -->
